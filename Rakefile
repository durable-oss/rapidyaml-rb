# frozen_string_literal: true

#
# Tasks are loaded from `rakelib/*.rake`.
# Run `rake -T` to see what's available.
#
require 'bundler'
GEMSPEC = Bundler.load_gemspec('rapidyaml.gemspec')

task default: %i[compile test]
